[package]
name = "libsrtp_rs"
version = "0.1.0"
authors = ["Richard Barnes <rlb@ipv.sx>"]
edition = "2018"

[lib]
crate-type = ["staticlib"]

[profile.test]
opt-level = 0
debug = true

[features]
rust-crypto = ["aes", "aes-gcm", "cipher", "constant_time_eq", "ctr", "hmac", "sha-1"]
openssl-crypto = ["openssl", "openssl-sys"]
cffi = ["libc", "cstr", "cpu-time", "rand", "aes", "sha-1"]
default = ["cffi", "rust-crypto"]

[dev-dependencies]
hex = "0.4.3"
rand = "0.8.3"

[dependencies]
vob = "3.0.0"              # Bit vector for extended replay database
num_enum = "0.5.1"         # For enum conversion to numeric values
packed_struct = "0.5"      # For RTP header parsing
hex-literal = "0.3.3"      # For test vector literals

# C FFI dependencies
cpu-time = { version = "1.0.0", optional = true }  # For cipher performance testing in C interface
cstr = { version = "0.2.8", optional = true }      # For C string constants
libc = { version = "0.2.93", optional = true }     # Only until we can get rid of src/c/alloc.rs
rand = { version = "0.8.3", optional = true }      # For rand_for_tests functions

# Rust Crypto dependencies
aes = { version = "0.7.4", optional = true }
aes-gcm = { version = "0.9.2", features = ["aes"], optional = true }
cipher = { version = "0.3.0", optional = true }
constant_time_eq = { version = "0.1.5", optional = true }
ctr = { version = "0.7.0", optional = true }
hmac = { version = "0.11.0", optional = true }
sha-1 = { version = "0.9.6", optional = true }

# OpenSSL dependencies
openssl = { version = "0.10", optional = true }
openssl-sys = { version = "0.9.66", optional = true }
